# Development / CI dependencies.
#
# Pinned with upper bounds (matching requirements.txt policy) so local and CI
# environments stay reproducible and don't drift onto a breaking tool major.
-r requirements.txt

pytest>=7.0.0,<9.0.0
pytest-cov>=4.0.0,<7.0.0
mypy>=1.0.0,<2.0.0
ruff>=0.5.0,<1.0.0
pylint>=3.0.0,<4.0.0
bandit>=1.7.0,<2.0.0
radon>=6.0.0,<7.0.0
